// Declare the URL string variable
const url: string = "https://www.cefalo.com/portal/";

// Define the base URL
const baseUrl: string = "https://www.cefalo.com";

// Check if the URL is equal to the base URL
const isBaseUrl: boolean = url === baseUrl;

// Print the result
console.log(isBaseUrl);
